package com.example.chen.snailweather.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.chen.snailweather.db.ProvincesDB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PROVINCES_DB".
*/
public class ProvincesDBDao extends AbstractDao<ProvincesDB, Long> {

    public static final String TABLENAME = "PROVINCES_DB";

    /**
     * Properties of entity ProvincesDB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ProvincesName = new Property(1, String.class, "provincesName", false, "PROVINCES_NAME");
        public final static Property ProvincesCode = new Property(2, int.class, "provincesCode", false, "PROVINCES_CODE");
    }


    public ProvincesDBDao(DaoConfig config) {
        super(config);
    }
    
    public ProvincesDBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PROVINCES_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"PROVINCES_NAME\" TEXT NOT NULL ," + // 1: provincesName
                "\"PROVINCES_CODE\" INTEGER NOT NULL );"); // 2: provincesCode
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PROVINCES_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ProvincesDB entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getProvincesName());
        stmt.bindLong(3, entity.getProvincesCode());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ProvincesDB entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getProvincesName());
        stmt.bindLong(3, entity.getProvincesCode());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ProvincesDB readEntity(Cursor cursor, int offset) {
        ProvincesDB entity = new ProvincesDB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // provincesName
            cursor.getInt(offset + 2) // provincesCode
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ProvincesDB entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setProvincesName(cursor.getString(offset + 1));
        entity.setProvincesCode(cursor.getInt(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ProvincesDB entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ProvincesDB entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ProvincesDB entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
