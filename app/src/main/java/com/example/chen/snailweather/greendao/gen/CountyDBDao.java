package com.example.chen.snailweather.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.chen.snailweather.db.CountyDB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "COUNTY_DB".
*/
public class CountyDBDao extends AbstractDao<CountyDB, Long> {

    public static final String TABLENAME = "COUNTY_DB";

    /**
     * Properties of entity CountyDB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property CountyName = new Property(1, String.class, "countyName", false, "COUNTY_NAME");
        public final static Property CountyCode = new Property(2, int.class, "countyCode", false, "COUNTY_CODE");
        public final static Property CityId = new Property(3, int.class, "cityId", false, "CITY_ID");
    }


    public CountyDBDao(DaoConfig config) {
        super(config);
    }
    
    public CountyDBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"COUNTY_DB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"COUNTY_NAME\" TEXT NOT NULL UNIQUE ," + // 1: countyName
                "\"COUNTY_CODE\" INTEGER NOT NULL ," + // 2: countyCode
                "\"CITY_ID\" INTEGER NOT NULL );"); // 3: cityId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"COUNTY_DB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CountyDB entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getCountyName());
        stmt.bindLong(3, entity.getCountyCode());
        stmt.bindLong(4, entity.getCityId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CountyDB entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getCountyName());
        stmt.bindLong(3, entity.getCountyCode());
        stmt.bindLong(4, entity.getCityId());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CountyDB readEntity(Cursor cursor, int offset) {
        CountyDB entity = new CountyDB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // countyName
            cursor.getInt(offset + 2), // countyCode
            cursor.getInt(offset + 3) // cityId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CountyDB entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCountyName(cursor.getString(offset + 1));
        entity.setCountyCode(cursor.getInt(offset + 2));
        entity.setCityId(cursor.getInt(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CountyDB entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CountyDB entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CountyDB entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
